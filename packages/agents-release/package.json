{
  "name": "@flao/agents-release",
  "version": "1.0.0",
  "description": "Release Agent for FLAO - Versioning and release notes",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@flao/core": "workspace:*",
    "@flao/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}

